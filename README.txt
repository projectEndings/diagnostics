== Notes for the README ==

This process requires oXygen 17.0 or higher [[the ask variable processes before all the other editor variables do, so having ${home} as a default value just returns ${home} in anything before oXygen 17.0. 

Should this README be generated?